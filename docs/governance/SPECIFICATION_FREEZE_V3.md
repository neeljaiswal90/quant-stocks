# Specification freeze policy V3 and export V2

Policy V3 selects the exact NEE-172 operational V2 bundle while preserving every
active blocker from policy V2. It corrects the stale calendar description to XNAS
and states that the remaining set spans governance, evidence, and engineering.

The deterministic export is a hash-verified, blocked snapshot. Neither the component
publication receipt nor this package is the final NEE-121 freeze receipt. Integrity
does not imply methodological approval, production evidence, empirical performance,
downstream authorization, or M0 completion.
