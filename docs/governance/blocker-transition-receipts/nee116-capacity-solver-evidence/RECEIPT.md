# NEE-116 capacity-solver evidence transition receipt

This candidate binds the exact PR #58 V3 remediation bytes, their fresh independent GO, the prior exact-byte owner signoff on those remediation bytes, and the successful protected-main publication receipt.

It proposes one append-only successor transition: remove only the verbatim `NEE-116-CAPACITY-SOLVER` engineering-evidence row from Freeze V6, retain the other nine active rows byte-for-byte in the same order, and append the blocker code to historical resolved/superseded lineage. The arithmetic is 10→9 active and 20→21 historical.

The bounded basis is executable fail-closed engineering conformance. It is not empirical portfolio capacity. NEE-116 remains In Progress because four sibling blocker rows remain active. Every inherited statistical and production claim remains unchanged, including `portfolio_capacity_available=false`, `milestone_m0_complete=false`, `production_ready=false`, and `live_order_authority=false`.

These candidate bytes do not perform the transition. Freeze V6 remains authoritative until this exact 13-file delta receives fresh independent review, separate exact-byte owner signoff, unchanged merge, and protected-main CI.
