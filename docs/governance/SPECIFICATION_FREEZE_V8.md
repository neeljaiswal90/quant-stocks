# Specification Freeze V8 candidate

Status: `M0_COMPLETE_0_ACTIVE_FINAL_FREEZE` after, and only after, separate exact-byte owner acceptance, unchanged protected publication, and successful protected-main CI.

This append-only candidate supersedes no Freeze V7 bytes. It binds the exact PR #60 M0 substantive-evidence packet, its fresh detached exact-head independent GO, the owner's packet signoff, the unchanged merge, both protected-main success receipts, and the final one-version M0 completion directive.

All nine Freeze V7 rows are appended to resolved/superseded lineage in their existing order. The directive supplies cross-contract semantic approval, and its durable timestamp anchors the final freeze. Arithmetic: 9→0 active and 21→30 historical.

Claims change only for final M0 closure and registered implementation availability. Empirical performance and capacity, production readiness, prospective-observation consumption, and live-order authority remain false.

These files are one final review candidate. Freeze V7 remains authoritative until a fresh independent delta review returns GO, the owner locks these exact bytes, they merge unchanged, and protected-main CI succeeds. No V9 or V10 transition is planned.

Use `verify_specification_freeze_v8` for fail-closed replay and `serialize_specification_freeze_v8_export` only with its verifier-created opaque result. The verifier binds the predecessor and M0 candidate manifests, all 65 candidate inventory leaves, receipts, directive, freeze anchor, exact-const schemas, raw and semantic hashes, the exact transition, bounded claims delta, and its normalized self-hash.
