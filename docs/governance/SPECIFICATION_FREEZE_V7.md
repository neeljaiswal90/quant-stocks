# Specification Freeze V7 candidate

Status: `BLOCKED_9_ACTIVE_GOVERNANCE_EVIDENCE_AND_ENGINEERING` after, and only after, separate exact-byte owner acceptance, unchanged protected publication, and successful protected-main CI.

This append-only candidate supersedes no Freeze V6 bytes. It binds the exact PR #58 V3 capacity remediation, the fresh independent GO on those bytes, the prior exact-byte remediation signoff, and the protected-main success receipt. It proposes removal of only `NEE-116-CAPACITY-SOLVER`.

The other nine active blocker rows remain verbatim and in the same order. Historical resolved/superseded lineage appends exactly one code. Arithmetic: 10→9 active and 20→21 historical.

The accepted scope is engineering conformance: parameter domains fail closed before bound computation, while valid-domain behavior preserves immutable V2 exact-rational results. No empirical portfolio capacity value exists. The complete Freeze V6 claims object is copied unchanged; M0, production readiness, live-order authority, and empirical capacity remain false. NEE-116 remains In Progress because its other blocker rows are unaffected.

The exact 13 new files are a review candidate only. Freeze V6 remains authoritative until a fresh independent delta review returns GO, the owner signs these exact bytes, they merge unchanged, and protected-main CI succeeds. This candidate cannot itself clear a blocker, complete NEE-116 or M0, authorize production, or grant live-order authority.

Use `verify_specification_freeze_v7` for fail-closed replay and `serialize_specification_freeze_v7_export` only with its verifier-created opaque result. The verifier binds predecessor and candidate manifests, candidate lineage, receipts, exact-const schemas, raw artifact hashes, semantic hashes, the exact transition, unchanged claims, and its normalized self-hash.
